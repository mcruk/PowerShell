This a modified version of deploy that truly runs in parallel.  open, click debug and parallel.

There is a bug, when adding computer the last one must be listed twice.

Parallel scripts must be formatted like Install_Install Delprof.ps1